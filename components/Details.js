import React, { Component } from 'react';
import { ScrollView } from 'react-native';
import { Title, List, ListITem } from 'react-native-elements';

export default class Details extends Component {
  render() {
    return <ScrollView>

    </ScrollView>;
  }
}
